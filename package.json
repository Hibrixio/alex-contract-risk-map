{
  "name": "orbit-contract-mind-map",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "dev": "node dev-server.cjs",
    "build": "node build.cjs",
    "test:browser": "playwright test"
  },
  "dependencies": {
    "jose": "^6.2.12",
    "tesseract.js": "^6.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
